# 第二阶段迁移与验收记录

日期：2026-08-03

## 完成范围

- 将 DeepSeek Codex 原项目的完整前端交互主体迁入“AI参谋 → 在线参谋”。
- 保留“砚都跨境”外层主导航与页面入口。
- 使用 Shadow DOM 隔离原项目样式，避免污染主应用其它页面。
- 补齐分支切换与图片克隆两个缺失桌面桥接接口。
- 保留现有 OpenAI 接入，并在模型菜单显示全名“OpenAI ChatGPT Latest”。

## 源码验收

- 原项目调用的 24 个桌面 API 方法，在迁移界面中全部存在；源/目标方法集合 diff 为空。
- 功能标记检查全部通过：项目改名、隐藏与恢复、搜索、消息编辑重跑、分支切换、拖放/粘贴图片、图片预览、工具记忆、运行中追加指令、图片克隆。
- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- 构建产物已确认包含：`OpenAI ChatGPT Latest`、`完全访问权限`、`搜索项目或对话`、`保存并重新执行`。

## 实际运行界面验收

在重新启动的 Electron 生产构建中，实际进入“AI参谋 → 在线参谋”检查：

- 项目：显示当前项目“砚都跨境”，历史任务正常载入。
- 搜索：点击后出现“搜索项目或对话…”输入框。
- 项目管理：菜单可操作，包含新建任务、访达显示、修改显示名称、侧边栏隐藏。
- 消息编辑：点击编辑后出现编辑框，以及“取消 / 保存并重新执行”。
- 模型：菜单可操作，显示 DeepSeek V4 Flash、DeepSeek V4 Pro、OpenAI ChatGPT Latest。
- 权限：菜单可操作，显示请求批准、替我批准、完全访问权限。
- 个性化：弹窗可打开，个性、自定义指令、记忆、工具结果记忆和重置控件均已加载。
- 图片：底部图片按钮可见，拖放/粘贴处理器与预览逻辑已迁入。

实际界面截图：`phase-2-ui.png`。

## 外部服务状态

界面与执行桥接已加载，但现有历史任务显示 DeepSeek 上游代理曾返回 HTTP 502（本机 127.0.0.1:10101 provider unreachable）。本阶段未将外部模型服务可用性误判为迁移功能通过；模型端到端生成仍取决于对应供应商额度与代理状态。

