# 阶段 7 验收：Amazon 真实数据源与失败降级

- 验收日期：2026-08-10
- 验收对象：当前用户 Amazon 数据源配置、选品分析师真实样本链路、API 失败降级链路
- 安全说明：验收文件不记录 API Key；端到端测试只复制加密配置到隔离临时目录，不修改真实会话与配置。
- 结论：**20/20 通过，完成率 100%**

## 真实外部数据源

1. 当前配置已启用：通过（US，1 页，每词最多 24 条，缓存 24 小时）。
2. 连接测试：通过，真实接口返回 48 条样本。
3. `waterless pet shampoo`：通过，原始 48，唯一并截取 24。
4. `no rinse pet cleanser`：通过，原始 48，唯一并截取 24。
5. `waterless pet body wash`：通过，原始 48，唯一并截取 24。
6. 真实缓存：通过，同词第二次请求 `cacheHit=true`。
7. 真实标题规范化：通过，HTML 实体已还原，不再显示 `&amp;`。
8. 三词真实端到端抓取：通过，原始 72，ASIN 去重 47。
9. 可比性分类：通过，DIRECT 32，ADJACENT 3，NON_COMPARABLE/排除 12。
10. 本品身份保留：通过，报告仍为“宠物免洗擦浴精华 / 液体精华”，宠物湿巾仅作为 ADJACENT，不回填本品形态。

## 失败降级与伪结论拦截

11. HTTP 503 错误暴露：通过，返回“第 1 页 HTTP 503”而不是空成功。
12. 三组 API 请求完整尝试：通过。
13. API 失败后浏览器后备：通过，三词均进入后备链路。
14. 两路无数据审计：通过，界面显示原始 0、成功 0/3、完整率 0%、低置信度。
15. 低样本门禁进入模型请求：通过，明确禁止无条件 `✅ 建议入场`。
16. 降级报告：通过，只输出“❓ 数据不足，不能判定”并列出补数任务。
17. 进度状态：通过，报告返回后不再显示“正在抓取”。
18. 生产资源：通过，实际加载 `index-DAK9lpGx.js` 和 `index-ChUd0jgO.css`。

## 工程与回归

19. TypeScript 检查 + 生产构建：通过。
20. 阶段 5 完整性回归：14/14 通过（分页、去重、缓存、百分位、低数据拦截、窄屏布局、最新资源）。

## 验收证据

- `live-probe.json`：真实连接、3 组检索和缓存证据。
- `live-ui-report.json`：真实数据源端到端界面验收。
- `failure-ui-report.json`：HTTP 503 失败降级验收。
- `05-live-report-title.png`：真实数据下的本品标题与形态。
- `06-live-sample-audit.png`：真实样本审计卡。
- `02-live-source-failure-report-top.png`、`03-live-source-failure-decision.png`、`04-live-source-failure-audit.png`：失败降级界面证据。

## 尚未扩展的边界

- 当前用户配置为 1 页 / 每词最多 24 条；系统已支持 2 页 / 48 条，但本阶段未擅自修改用户配置。
- Amazon 页面结构和第三方 API 可能变化；运行时已有错误暴露、浏览器后备和数据不足门禁。
