# 阶段 5 验收：Amazon 样本完整率与报告数据质量门禁

验收日期：2026-08-10

## 实现结果

- `Amazon 数据源配置`的页数、最大样本数和缓存时长已真正驱动 API 抓取和浏览器备用抓取。
- API 支持 1–2 页采集、跨页 ASIN 去重、最多 48 条截断、按配置时长缓存；保存或清除配置会清空旧缓存。
- Amazon 样本审计显示原始样本、自然位、赞助位、ASIN 去重、DIRECT、ADJACENT、已排除、检索词成功率、核心字段覆盖率和综合完整率。
- 纯赞助位不进入竞品统计；价格按 DIRECT 样本输出 P25、中位数、P75、区间和均值。
- 未抓取的销量、销售额和趋势必须标记 `U / 待验证`。
- 样本完整率低于 80% 时，后置质量门禁会拦截伪 TOP50、无证据销量/销售额/趋势和无条件“建议入场”，且不保存为正式报告。

## 真实执行的验收

| 验收组 | 结果 |
| --- | --- |
| 阶段 5 完整率/质量门禁逻辑 | 28/28 通过 |
| 阶段 4 可比性回归 | 28/28 通过 |
| 商品提取与身份锁回归 | 46/46 通过 |
| TypeScript 检查 | 通过 |
| 生产构建 | 通过 |
| 真实 Electron 主进程 + 渲染界面 | 14/14 通过 |
| 实际界面截图视觉检查 | 3/3 通过 |

阶段 5 实施范围完成率：**100%**。

## Electron 关键证据

- 配置保存：`pages=2`、`maxSamples=48`、`cacheHours=12`。
- 两页原始返回 60 条，跨页去重并按上限输出 48 条。
- 第二次相同查询 `cacheHit=true`，HTTP 请求数不增加。
- 决策级样本：原始 54、自然位 48、赞助位 6、DIRECT 36、ADJACENT 6、排除 6，完整率 100%。
- 低数据样本：DIRECT 3，核心字段覆盖 33%，完整率 33%；伪 TOP50、精确月销量和无条件入场同时被拦截。
- Electron 实际加载当前生产资源：`index-CuLq7hAt.js` 和 `index-DGiKp8lD.css`。

## 边界说明

本次用本地受控 HTTP 服务器模拟 Omkar/Amazon API 返回，但请求经过的是应用真实主进程 handler、安全存储的 API Key、分页、去重、截断、缓存、preload 和真实渲染界面。这验收了应用链路，**不代表外部 Omkar/Amazon 服务当前一定在线、不限流或不反爬**。
