- 所有对外部服务的构造均从 process.env 读取密钥与 baseUrl，并在缺失时给出明确错误提示
- 跨进程通信的数据结构统一来自 ../shared/contracts 的类型声明，禁止在主进程中重新定义接口
- 涉及用户敏感信息（eBay 令牌、密码）一律通过 electron safeStorage 加密存储后再落库
- 异步任务执行前后通过 appendEbayPublishAudit 追加审计事件，保证可追溯性
- 文件路径统一基于 app.getPath('userData') 拼接子目录，并使用 safeLocalSegment 做安全截断