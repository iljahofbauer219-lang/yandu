启动前需准备 `.env.local`（优先从 `app.getAppPath()` 或 `process.cwd()` 加载），配置 `EBAY_CLIENT_ID`/`EBAY_CLIENT_SECRET`/`EBAY_RUNAME`、`BAILIAN_API_KEY`/`BAILIAN_BASE_URL`、`DEEPSEEK_*`、`ARK_*`、`VOLC_TTS_*` 等环境变量；eBay 授权流程会弹出独立 BrowserWindow 捕获回调 URL 中的 `code` 和 `state`。