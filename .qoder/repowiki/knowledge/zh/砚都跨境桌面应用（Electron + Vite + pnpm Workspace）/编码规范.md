- 所有 TypeScript 源码遵循 strict 模式与 ES2022 target，通过 allowImportingTsExtensions 支持 .ts 直接导入
- 共享类型与工具放在 shared_code，由 renderer 与 main_process 共同引用，避免重复定义
- 构建产物统一输出到 dist 目录，渲染器输出 dist/renderer，主进程输出 dist/main，由根 package.json 的 main 字段指向最终入口
- 原生依赖通过 pnpm-workspace.yaml 的 allowBuilds 白名单显式授权，避免安装时阻塞