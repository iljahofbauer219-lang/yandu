import { EbayImageGroundingService } from '../src/main/services/EbayImageGroundingService'

function assert(condition:unknown,message:string):asserts condition{if(!condition)throw new Error(message)}
;(async()=>{const service=new EbayImageGroundingService('test-key','https://example.test','mock-model') as any,base={newStructures:[],missingStructures:[],identityScore:95,structuralScore:95,factScore:95,purposeScore:95,diversityScore:100,geometryScore:95,styleScore:90,geometryMismatch:false,status:'PASSED',reason:'mock'};const request={title:'商品',description:'任务',itemSpecifics:[],purpose:'PRODUCT' as const,candidateUrl:'https://example.test/candidate.png',sourceImages:['https://example.test/source.png'],sourceLabels:['主图'],referenceIndices:[0],protectedAttributes:['结构'],verifiedFacts:['商品'],shotInstruction:'D01',styleInstruction:'STYLE CONTRACT',targetLanguage:'俄语',baseImageNoMarketingText:true};service.chat=async()=>({...base,languageScore:40});const rejected=await service.reviewCandidate(request);assert(rejected.status==='REJECTED'&&rejected.languageScore===40,'底图语言分低于60必须拒绝');service.chat=async()=>({...base,languageScore:90});const passed=await service.reviewCandidate(request);assert(passed.status==='PASSED'&&passed.languageScore===90,'无字底图语言分达到80必须通过');console.log(JSON.stringify({badLanguageStatus:rejected.status,badLanguageScore:rejected.languageScore,cleanBaseStatus:passed.status,cleanBaseScore:passed.languageScore},null,2))})().catch(error=>{console.error(error);process.exit(1)})
